# SOKOLENOK v50.5 — hidden production fixes

## What changed

- Fixed short profile links (`/u/:steamid`) so Telegram/VK/Discord receive the generated SOKOLENOK OG card instead of a small Steam avatar.
- Updated the server health/version marker from the stale `v49.0.0` value to `v50.5.0`.
- Bumped package metadata to `5.0.5` so deployment checks match the archive version.

## Checks

- `npm.cmd run check`
