# Secrets — NEVER commit
.env
.env.*
!.env.example

# Runtime data (sqlite, json fallback, uploads, message key)
.data/
*.sqlite
*.sqlite-journal
*.db
.msgsecret

# Node
node_modules/
npm-debug.log*

# Build artifacts / archives
*.zip

# OS
.DS_Store
Thumbs.db

# Certbot (deploy)
deploy/certbot/
